/**
 * @deprecated Lazy Teammate Mode was removed — it injected prompts that refused all coding.
 * Kept as an empty export so any stale dynamic import cannot load harmful instructions.
 */
export const LAZY_TEAMMATE_RULES = ""
